# ovpn 项目指引

## 项目目标

本仓库是面向 Debian 13 的独立 OpenVPN Community 服务端管理器。
项目提供独立安装、配置、验证和恢复，不依赖其他源码仓库或父目录文件。
入口为`ovpn.sh`，安装后的命令为`ovpn`。

## 开始工作

修改前依次完成以下检查：

- 完整阅读本文件、`README.md`和`DESIGN.md`，涉及原生恢复流程时同时阅读`MANUAL.md`。
- 搜索已有实现、公开接口和全部调用方，优先更新现有模块。
- 分别检查 Git 工作区和暂存区，保留用户已有修改。
- 只有任务开始前遗留的未暂存修改需要先询问用户；已有暂存内容不阻止继续工作。
- 明确目标 Debian 版本、验证条件、系统副作用和回滚入口。

## 产品与设计原则

项目以用户维护的配置文件和完整模板为中心，不提供大量字段级编辑命令。
用户通过`config/ovpn.env`、服务端模板和客户端模板表达策略，CLI 负责定位、渲染、校验、部署和生命周期管理。

实现保持范围最小、可审查且可回滚，不为兼容未设计的平台或旧版本引入迁移层。
不因代码量减少而认定复杂度降低，也不自行替代 Easy-RSA、OpenSSL、OpenVPN、systemd 或 nftables 已承担的成熟职责。

操作必须幂等。
重复执行不得重复追加配置、创建等价资源或无条件重启服务；无法安全恢复时停止并报错。

## 文档规则

`README.md`面向使用者，只说明目标、特性、快速开始和常用命令，不堆放内部实现。
除非用户明确要求，AI 不修改`README.md`或`README_en.md`；发现 README 与实现或设计不符时应指出并交由用户决定。

`DESIGN.md`是完整详细设计，不再依赖仓库外的公共设计文件。
实现变化应最小化同步`DESIGN.md`；发现设计冲突或需要改变核心行为时先请用户决定。

`MANUAL.md`必须在管理器源码完全不存在时仍可独立使用。
它按公开命令或不可拆分的操作组织，给出 Debian 13 原生命令、前置条件、副作用、验证和恢复步骤，不要求从本仓库复制模板、hook 或 unit。
公开命令或主要副作用变化时同步检查`MANUAL.md`。

文档不在句子中间折行，一句话结束后可以折行。
文档不使用带序号的标题或列表。
中英文 README 的功能、命令和默认行为保持一致。

## 公开接口与帮助

无参数、`-h`和`--help`显示简短帮助，`help`显示完整帮助。
无参数不得执行系统操作。

简短帮助列出完整命令总览和全局参数，不展开执行过程。
完整帮助说明关键参数、默认行为、重要副作用、验证条件和恢复方式。
高风险命令必须明确影响和恢复入口。

`install`只安装管理器入口、模块和静态资源，不安装或启动 OpenVPN。
`core install`安装 OpenVPN 及运行依赖。
`core start`和`core restart`必须先执行与`core test`相同的检查。
外部程序没有无副作用的完整配置校验时，不得把局部静态检查描述为完整验证。

全局`--dir`只改变管理数据目录，不改变固定的 OpenVPN 运行目录和 systemd 服务名。
`--dry-run`不得写文件、签发或吊销证书、启动编辑器、修改网络或启停服务。

## 审计与可手动操作

所有修改系统状态的命令必须显示可审计的系统命令和文件 A/M/D 事件。
审计和 dry-run 应尽量按真实执行顺序展示，使使用者可以据此人工复现操作。

帮助等无系统交互的命令可以不显示审计。
`--no-audit`只关闭末尾审计摘要，不改变实际操作。
日志、错误、dry-run 和审计不得输出密码、摘要、私钥、证书正文或完整客户端配置。

## 凭据与个人信息

禁止提交密码、Token、私钥、真实证书、完整客户端配置、真实公网 IP、非公开域名、家庭网络拓扑或个人设备名。
示例使用`example.com`、文档保留地址和明显占位符。

PKI、客户端私钥、tls-crypt-v2 密钥和认证数据库必须使用设计规定的最小权限。
服务运行时只读取`/etc/openvpn/server`中的最小运行副本，不得为了服务访问而放宽整个管理 PKI 的权限。

## 安全、事务与恢复

写入前必须完成整份候选输入校验。
配置和运行材料应先写入安全临时目录，再以最终属主和权限部署。
reload、restart 或 start 前先完成当前可用的配置检查。

失败后停止后续依赖步骤，恢复本次事务前的文件和服务状态，并说明已完成的副作用、恢复结果及可复制的恢复命令。
删除只针对工具明确拥有的固定路径，不使用宽泛 glob，不删除未知文件。

长期备份放在受管备份目录，不散落在原文件旁。
新建备份后按设计清理：最长保留两个月，最多保留五十份。
普通 apply 使用临时事务副本；强制 CA 初始化和 purge 使用长期备份。

涉及可能中断远程连接的网络、OpenVPN 或防火墙操作时，先确认恢复入口。
不得自动执行真实网络 apply、合并 PR 或修改默认分支。

## 宽进严出

只读列举、状态和导出遵循宽进原则，不因未知字段、字段顺序或可由默认值补全的缺失字段中止整个命令。
写入工具管理的标准格式时严格校验输入、身份、文件类型、权限和目标边界。

未知内容不得在结构化转换时静默丢失。
只有重新生成的实际配置正文与原文逐字节相同时，才允许更新规范元数据；否则原样保留、原样导出或停止修改。
宽进不放宽符号链接、路径越界、凭据保护和受管对象归属等安全边界。

## systemd、降权与运行材料

管理器以 root 读取成功不能证明 OpenVPN 服务可用。
涉及 systemd 沙箱、服务降权、证书、CRL、认证 hook、防火墙或内核参数时，分别检查管理进程可访问、服务启动时可访问和服务降权后可访问。

apply 必须把服务所需的 CA、服务端证书、服务端私钥、CRL、tls-crypt-v2 密钥和认证脚本部署到固定运行目录，并纳入比较、事务回滚和卸载边界。
静态检查、dry-run、候选配置检查、真实服务启动和端到端握手必须分层报告结论。

## Shell 编码规则

- 可执行脚本使用`#!/usr/bin/env bash`和`set -Eeuo pipefail`；被 source 的模块不自行修改调用者的 shell 选项。
- 文件使用 UTF-8、LF 换行并保留末尾换行。
- 使用脚本自身目录定位模块和资源，不依赖当前工作目录。
- 变量展开默认使用双引号；函数局部变量使用`local`，模块常量使用`readonly`。
- 使用`[[ ... ]]`判断字符串和文件，使用`(( ... ))`判断整数。
- 输出使用`printf`，错误写入 stderr，正常结果写入 stdout。
- 外部命令应检查依赖，不解析面向人类的非稳定输出。
- 路径和配置值不得假设不含空格或通配符，支持时使用`--`结束选项解析。
- 临时路径使用安全创建方式并由幂等 trap 清理。
- 不使用`eval`；允许失败的命令通过`if`、`case`或明确恢复逻辑处理，不随意使用`|| true`。
- 参数解析遇到未知参数、缺少值或多余位置参数时返回非零状态。
- ShellCheck 抑制限制在最小范围，并注明原因。
- 修改公共函数、参数、输出或路径时同步搜索所有调用方、文档和测试。

## 重写与完成判定

重写已有可用能力前，先列出公开命令、默认行为、文件权限、运行路径、服务身份、网络副作用、凭据生命周期、失败状态和恢复入口，形成逐项回归清单。
有意删除或改变旧行为时在设计中明确记录，不能把遗漏当作简化。

只有公开功能回归、失败恢复和 Debian 13 真机集成条件已经覆盖或逐项列为未验证，才能声称功能完成。
真机问题不能由静态测试或 dry-run 替代。

## 最低验证

修改后根据范围执行：

- 所有修改过的 Shell 脚本运行`bash -n`。
- 系统存在 ShellCheck 时运行`shellcheck`。
- 运行`bash tests/test-cli.sh`。
- 检查`ovpn.sh --help`和`ovpn.sh help`。
- 修改模块时检查对应 help 和 dry-run。
- 搜索失效路径、父目录依赖和仓库外源码引用。
- 检查审计、备份、权限、服务降权可访问性和回滚。
- 运行`git diff --check`。

无法在当前环境验证真实 systemd、OpenVPN 握手、证书吊销、认证 hook、nftables、IPv4 转发或网络连通性时，交付说明必须逐项列出未验证内容。

## 完成修改

完成代码修改和验证后检查 Git 暂存区。
暂存区为空时运行`git add -A`，把当前工作区全部修改加入暂存区。
暂存区已有内容时不自动调整或追加，避免干扰用户选择的提交范围。

生成 commit message 前必须重新检查暂存区。
commit message 必须覆盖暂存区全部修改，不能只描述最后编辑的文件。
可以保留 Conventional Commits 类型和英文 scope，但冒号后的主题及可选正文使用中文，例如`feat(install): 增加独立配置命令`。

完成后明确说明已验证内容、未验证内容和剩余风险。
